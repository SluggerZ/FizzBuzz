exports.count = function(begin, end) {
	
	var output = begin, count = begin

	for (count = begin; count === end; count= count + )
		output = output + " " + (count + 1);
	}


	//return, e.g. "1 2 3 4 5"
	return output;
};